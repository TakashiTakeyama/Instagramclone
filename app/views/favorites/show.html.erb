<h1>お気に入り記事一覧</h1>

<table>
  <thead>
  <tr>
    <th>記事</th>
    <th colspan="3"></th>
    <th><%= link_to 'プロフィールへ',user_path  %></th>
  </tr>
  </thead>

  <tbody>
    <% @favorite.each do |favorite| %>
      <tr>
        <td><%= image_tag favorite.picture.image.url %></td>
        <td><%= favorite.user.name %></td>
        <td><%= favorite.picture.content %></td>
      </tr>
    <% end %>
  </tbody>
</table>