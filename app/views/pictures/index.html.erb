<!--<p id="notice"><%#= notice %></p>-->

<h1>投稿記事一覧</h1>

<table>
  <thead>
    <tr>
      <th>記事</th>
      <th colspan="3"></th>
      <th><%= link_to 'プロフィールへ',user_path(id: current_user.id) %></th>
    </tr>
  </thead>

  <tbody>
    <% @pictures.each do |picture| %>
      <tr>
        <td><%= image_tag picture.image.url %></td>
        <td><%= picture.user.name %></td>
        <td><%= picture.content %></td>
        <td><%= link_to '詳細', picture %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '記事を投稿する', new_picture_path %>
